This code is fairly unstable
The code may changed from here

I will described added Additions at a later date

If you want to test it, download the raw zip file from "code" and extract it
Then extract the file, open it, select all the elements in the file and zip them
